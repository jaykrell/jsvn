#include <stdio.h>#include <stdlib.h>#include "codefragments.h"#include "errmgr.h"int main(int argc, char** argv, char **envp){printf("\n\n\n\n\n\n\nhello world\n");printf("%p\n", (void*)envp);if (envp){	printf("%p\n", (void*)*envp);	if (*envp)		printf("%p\n", (void*)**envp);	while (*envp && **envp)	{	  printf("%s=%s\n", *envp, *envp + 1 + strlen(*envp));	  envp += 1;	}}return 0;}